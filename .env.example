# 复制本文件为 .env.local 后填写真实值（不要把密钥写进 .env.example 再提交）
# Supabase：Project Settings → API
# URL 只填一行，例如 https://xxxxx.supabase.co（不要拼上多余文字）
VITE_SUPABASE_URL=https://你的项目ref.supabase.co
# anon / public 密钥：很长的 JWT，以 eyJ 开头（勿填成控制台网址，勿用 service_role）
VITE_SUPABASE_ANON_KEY=
